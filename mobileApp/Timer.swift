import SwiftUI

struct Timer: View {
    var time: Double
    var body: some View {
        ZStack {
        }
    }
}

struct Timer_Previews: PreviewProvider {
    static var previews: some View {
        Timer(time: 0.0)
    }
}
